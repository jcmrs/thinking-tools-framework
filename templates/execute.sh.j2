#!/usr/bin/env bash
# Auto-generated execution wrapper for {{ tool_name }}
# Generated by thinking-tools-framework Skills exporter

set -euo pipefail

TOOL_NAME="{{ tool_name }}"
COGITO_BIN="${COGITO_BIN:-cogito}"

# Execute thinking tool via CLI
# Passes all arguments to cogito execute command
exec "$COGITO_BIN" execute "$TOOL_NAME" "$@"
